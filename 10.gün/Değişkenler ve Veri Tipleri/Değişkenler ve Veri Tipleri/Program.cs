using System;

class Program
{
    static void Main()
    {/*int → tam sayı
string → metin

bool → doğru/yanlış

double → ondalıklı sayı 
        */
        int yas = 25;
        string isim = "Ahmet";
        bool aktifMi = true;
        double maas = 12345.67;

        Console.WriteLine("İsim: " + isim);
        Console.WriteLine("Yaş: " + yas);
        Console.WriteLine("Aktif mi? " + aktifMi);
        Console.WriteLine("Maaş: " + maas);
    }
}
